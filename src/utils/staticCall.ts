import { Contract } from '@ethersproject/contracts';

export const staticCall = (lineAddress: string, tokenAddress: string, network: string): string => {
  // const provider = new ethers.providers.JsonRpcProvider('https://mainnet.infura.io');
  // const contract = new Contract(lineAddress, abi, provider);
  // const result = await contract.functions.myStaticFunction();
  return '';
};
